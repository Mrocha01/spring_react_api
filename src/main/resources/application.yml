spring:
  datasource:
    url: jdbc:postgresql://localhost:55432/imageLiteApp
    username: postgres
    password: postgres
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
    show-sql: true